{
    "name": "taluu/link-set",
    "license": "MIT",
    "description": "Changeset calculator between data",
    "keywords": ["changeset"],

    "autoload": {
        "psr-0": {
            "LinkSet": "src/"
        }
    },

    "require": {
        "php": ">=5.4"
    },

    "require-dev": {
        "phpunit/phpunit": "~3.8.0"
    },

    "config": {
        "bin-dir": "bin"
    },

    "minimum-stability": "dev",
    "prefer-stable": true
}

